machine:
    node:
        version: 6.9.2

dependencies:
    post:
        - npm run install:modules:electron

test:
    post:
        - npm run build
        - npm run package
        - zip -r release.zip release
        - mv release.zip $CIRCLE_ARTIFACTS
